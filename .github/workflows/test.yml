# test.yml
name: Testing

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:

  pa11y-scan:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - name: Accessibility Scan
        run: npm run pa11y

